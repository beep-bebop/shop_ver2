<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.csu.shop.persistence.AccountMapper">
  <select id="getAccountByUsername" parameterType="String" resultMap="Account">
    select
      signon.username,
      account.email,
      account.firstname,
      account.lastname,
      account.status,
      account.addr1 as address1,
      account.addr2 as address2,
      account.city,
      account.state,
      account.zip,
      account.country,
      account.phone,
      profile.langpref as languagePreference,
      profile.favcategory as favouriteCategoryId,
      profile.mylistopt as listOption,
      profile.banneropt as bannerOption,
      bannerdata.bannername
    from account, profile, signon, bannerdata
    where account.userid = #{username}
      and signon.username = account.userid
      and profile.userid = account.userid
      and profile.favcategory = bannerdata.favcategory
  </select>
  <select id="getAccountByUsernameAndPassword" resultType="Account">
    select
      signon.username,
      account.email,
      account.firstname,
      account.lastname,
      account.status,
      account.addr1 as address1,
      account.addr2 as address2,
      account.city,
      account.state,
      account.zip,
      account.country,
      account.phone,
      profile.langpref as languagePreference,
      profile.favcategory as favouriteCategoryId,
      profile.mylistopt as listOption,
      profile.banneropt as bannerOption,
      bannerdata.bannername
    from account, profile, signon, bannerdata
    where account.userid = #{param1}
      and signon.password = #{param2}
      and signon.username = account.userid
      and profile.userid = account.userid
      and profile.favcategory = bannerdata.favcategory
  </select>

  <update id="updateAccount" parameterType="Account">
    update
      account set
      email = #{email},
      firstname = #{firstName},
      lastname = #{lastName},
      status = #{status},
      addr2 = #{address2,jdbcType=VARCHAR},
      city = #{city},
      state = #{state},
      zip = #{zip},
      country = #{country},
      phone = #{phone}
    where userid = #{username}
   </update>

  <insert id="insertAccount" parameterType="Account">
    insert into account
      (email, firstname, lastname, status, addr1, addr2, city, state, zip, country, phone, userid)
    values
      (#{email}, #{firstName}, #{lastName}, #{status}, #{address1},  #{address2,jdbcType=VARCHAR}, #{city}, #{state}, #{zip}, #{country}, #{phone}, #{username})
  </insert>

  <update id="updateProfile" parameterType="Account">
    <bind name="listOptionValue" value="_parameter.isListOption() ? 1 : 0" />
    <bind name="bannerOptionValue" value="_parameter.isBannerOption() ? 1 : 0" />

    update profile set
      langpref = #{languagePreference},
      favcategory = #{favouriteCategoryId},
      mylistopt = #{listOptionValue},
      banneropt = #{bannerOptionValue}
    where userid = #{username}
  </update>

  <insert id="insertProfile" parameterType="Account">
    <bind name="listOptionValue" value="_parameter.isListOption() ? 1 : 0" />
    <bind name="bannerOptionValue" value="_parameter.isBannerOption() ? 1 : 0" />

    insert into profile
      (langpref, favcategory, mylistopt, banneropt, userid)
    values
      (#{languagePreference}, #{favouriteCategoryId}, #{listOptionValue}, #{bannerOptionValue}, #{username})
  </insert>

  <update id="updateSignOn" parameterType="Account">
    update signon set password = #{password}
    where username = #{username}
  </update>

  <insert id="insertSignOn" parameterType="Account">
    insert into signon (password,username)
    values (#{password}, #{username})
  </insert>
</mapper>